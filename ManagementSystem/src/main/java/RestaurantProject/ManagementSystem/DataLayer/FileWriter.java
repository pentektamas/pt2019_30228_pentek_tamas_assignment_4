package RestaurantProject.ManagementSystem.DataLayer;

import java.io.FileOutputStream;
import java.io.IOException;
import java.io.ObjectOutputStream;

import RestaurantProject.ManagementSystem.BusinessLayer.MenuItem;

public class FileWriter {

	/*
	 * public ObjectOutputStream openFileForWrite(String filename) {
	 * ObjectOutputStream openedFile;
	 * 
	 * try { FileOutputStream file = new FileOutputStream(filename); openedFile =
	 * new ObjectOutputStream(file);
	 * 
	 * outputObject.close(); file.close();
	 * System.out.println("Serialization is done!"); return openedFile; } catch
	 * (IOException e) {
	 * System.out.println("InputOutput exception - serialization"); }
	 * 
	 * }
	 * 
	 * public void closeFile(ObjectOutputStream object, FileOutputStream file) { try
	 * { object.close(); file.close(); } catch (IOException e) {
	 * System.out.println("Error! - Closing a file"); } }
	 */
}
